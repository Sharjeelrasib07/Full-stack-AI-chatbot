{
  "name": "ai-chatbot-server",
  "version": "1.0.0",
  "description": "Express backend that proxies chat requests to the OpenAI API, so the API key never reaches the browser.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mammoth": "^1.12.2",
    "multer": "^2.3.0",
    "openai": "^4.68.0",
    "pdf-parse": "^2.4.5"
  }
}
