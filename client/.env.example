# Only needed when deploying. Locally, the app defaults to
# http://localhost:8000 automatically — you don't need this file for
# local development.
#
# When deployed, set this to your deployed backend's URL, e.g.:
# VITE_API_BASE_URL=https://your-backend.onrender.com
VITE_API_BASE_URL=
